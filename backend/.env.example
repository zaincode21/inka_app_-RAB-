DATABASE_URL="postgresql://inka:inka_password@localhost:5432/inka?schema=public"
PORT=4000
CORS_ORIGIN="*"
JWT_SECRET="replace-with-a-long-random-secret"
APP_PUBLIC_URL="http://localhost:4000"
# Optional: send real reset emails via Resend (otherwise codes are logged to the API console)
# RESEND_API_KEY=""
# MAIL_FROM="Inka <onboarding@resend.dev>"
# Set to false in production so forgot-password never returns the raw token in JSON
# EXPOSE_DEV_RESET_TOKEN="false"
SUPER_ADMIN_EMAIL="admin@inka.local"
SUPER_ADMIN_PASSWORD="admin123"
DEMO_OWNER_EMAIL="owner@inka.local"
DEMO_OWNER_PASSWORD="owner123"
